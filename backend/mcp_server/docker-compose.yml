version: '3.8'

services:
  question-mcp:
    build: .
    ports:
      - "8765:8765"
    environment:
      - KIMI_API_KEY=${KIMI_API_KEY}
      - LOG_LEVEL=INFO
    volumes:
      - ./data:/app/data
    restart: unless-stopped
